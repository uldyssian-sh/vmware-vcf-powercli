name: Weekly Activity

# Run every Sunday at midnight UTC
on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  weekly-activity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        
      - name: Weekly Activity
        id: activity-check
        run: |
          set -e
          echo "Weekly activity check completed"
          date > weekly-activity.log
          echo "completed=true" >> $GITHUB_OUTPUT
          
      - name: Commit changes
        id: commit-changes
        run: |
          set -e
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          if git diff --staged --quiet; then
            echo "No changes to commit"
            echo "changes=false" >> $GITHUB_OUTPUT
          else
            git commit -m "Weekly activity update"
            echo "changes=true" >> $GITHUB_OUTPUT
          fi
        continue-on-error: true
        
      - name: Push changes
        if: steps.commit-changes.outputs.changes == 'true'
        run: |
          set -e
          if ! git push; then
            echo "Failed to push changes - this may be expected"
            exit 0
          fi
        continue-on-error: true
        
      - name: Handle workflow failure
        if: failure()
        run: |
          echo "Weekly activity workflow encountered issues but continuing"
          echo "Check previous steps for specific error details"# Updated Sun Nov  9 12:50:15 CET 2025
